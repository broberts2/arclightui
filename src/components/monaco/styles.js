import styled from "styled-components";

const Container = styled.div`
  width: 100%;
  height: 100%;
  position: relative;
`;

const FormatButton = styled.div`
  position: absolute;
  right: 40px;
  bottom: 20px;
`;

export default {
  Container,
  FormatButton,
};
